use("ncit-data");

// db.student.find();

// ? delete student whose age is 35

//db.student.deleteMany({ age:35});

//db.student.find();


//delete students who are graduated and age is greater then 3

//db.student.deleteMany({ isGraduated: true, age: {$gt:3}});


// ? delete all students
// db.student.deleteMany({});
